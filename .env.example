# Create these with `npx convex dev`.
NEXT_PUBLIC_CONVEX_URL=
CONVEX_DEPLOYMENT=

# Create a Clerk application, then enable its Convex JWT template/integration.
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
CLERK_JWT_ISSUER_DOMAIN=organic-ewe-89.clerk.accounts.dev

# Required for text and image safety checks before cards can be posted.
OPENAI_API_KEY=
